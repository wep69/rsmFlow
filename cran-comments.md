## Development snapshot 0.2.0

This snapshot is not yet a CRAN submission candidate.

Known release gates:

* Replace placeholder maintainer email in DESCRIPTION.
* Execute testthat suite on R >= 4.2.
* Render all vignettes.
* Validate optional backends on Windows/Linux/macOS.
* Run frozen simulation battery and summarize numerical performance.
* Run R CMD build and R CMD check --as-cran.
* Refresh current package ecosystem and metadata snapshot immediately before release.
